
LIST NEW PROD PROD-1 PROD-2 PROD-3

FOR SMRY_ID IN RANGE(1, $NUM_CASES)
    FOR WELL IN $PROD
        DEFINE ${SMRY_ID}:WWCT2:${WELL} = ${WWPR:${WELL}}/(${WWPR:${WELL}}+${WOPR:${WELL}}) 
    NEXT
NEXT



FOR WELL IN $PROD
   FOR SMRY_ID IN RANGE(1, $NUM_CASES)
        ADD CHART
        ADD SERIES ${SMRY_ID} WWCT:${WELL}
        ADD SERIES ${SMRY_ID} WWCT2:${WELL}
   NEXT
NEXT
