
LIST NEW INJ INJ1 INJ2
LIST NEW PROD PROD-1 PROD-2 PROD-3 PROD-4

FOR WELL IN ${PROD}
    ADD CHART
    ADD SERIES ? WOPR:${WELL}
NEXT

FOR WELL IN ${INJ}
    ADD CHART
    ADD SERIES ? WBHP:${WELL}
NEXT
